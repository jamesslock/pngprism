name: CI

# The crate's own suite. This is NOT Project Prism's release gate — that runs
# lab-side (see README, "Where the evidence lives"). A green run here means the
# implementation agrees with its vendored Python oracle and its own corpora; it
# makes no claim about the 2,452-cell parity matrix or the 815-unit golden
# corpus, which this repository does not carry.

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:
  # Weekly re-run, mainly to catch drift the repository itself does not cause:
  # a new RUSTSEC entry against an unchanged Cargo.lock, a toolchain or action
  # that has moved, an external dependency that stopped resolving.
  #
  # This cron is NOT the advisory safety net, and must not be relied on as one.
  # GitHub disables scheduled workflows after 60 days without repository
  # activity, so it switches itself off in precisely the finished-and-untouched
  # case where an activity-independent check would matter most. That is a
  # documented limitation of the `schedule` event, not something this file can
  # work around.
  #
  # The real gate is Dependabot: security alerts and automated security fixes
  # are enabled on this repository. They run server-side, on GitHub's schedule
  # rather than this one, and keep working while the repository sits idle.
  schedule:
    - cron: "17 6 * * 1"   # Mondays, 06:17 UTC (off the hour: the top of the
                           # hour is heavily contended and jobs get delayed)

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    name: test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        # macOS is the development platform; Linux is here because a consumer
        # is more likely to be on it than not, and the crate claims portability
        # it has never had a machine to check.
        os: [macos-latest, ubuntu-latest]
    steps:
      - uses: actions/checkout@v4

      # rust-toolchain.toml pins the version; this action honors it.
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2

      # tests/caps.rs runs the vendored Python oracle as a subprocess and
      # asserts the port agrees with it — the parity rule's differential test.
      # Without Python that suite cannot run, so make its absence loud.
      - name: Check Python is available for the oracle parity suite
        run: python3 --version

      # `--pack max` shells out to zopflipng. It is genuinely optional — the
      # default pack mode is `none`, and the tests SKIP when it is missing so a
      # consumer without it does not see a red suite. That makes installing it
      # here load-bearing: without this step the `--pack max` path would skip on
      # every run and nothing would ever exercise it.
      - name: Install zopflipng (exercises the --pack max path)
        run: |
          if [ "$RUNNER_OS" = "macOS" ]; then
            brew install zopfli
          else
            sudo apt-get update && sudo apt-get install -y zopfli
          fi
          command -v zopflipng

      # Fail loudly rather than skipping quietly: on CI the tool must be present.
      - name: Assert the --pack max path will actually run
        run: test -n "$(command -v zopflipng)"

      - name: Test (debug)
        run: cargo test --locked --all-targets

      - name: Test (release)
        run: cargo test --locked --release

      - name: Doc tests
        run: cargo test --locked --doc

  lint:
    name: clippy + fmt + docs
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
      - uses: Swatinem/rust-cache@v2

      - name: Clippy (warnings are errors)
        run: cargo clippy --locked --all-targets -- -D warnings

      - name: Formatting
        run: cargo fmt --all -- --check

      - name: Rustdoc (warnings are errors)
        env:
          RUSTDOCFLAGS: -D warnings
        run: cargo doc --locked --no-deps

  surface:
    name: publication surface
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # This repository was extracted from a private research tree. The rule
      # that lab-only material never lands here used to be enforced by the
      # generator that assembled the public tree; that generator is retired, so
      # the rule is enforced here instead. Publishing is permanent — a file
      # added by accident cannot be recalled by deleting it later.
      - name: Top-level entries must all be declared
        shell: bash
        run: |
          declared="$(grep -v '^#' .github/publication-surface.txt | grep -v '^$' | sort -u)"
          actual="$(git ls-files | cut -d/ -f1 | sort -u)"
          if ! diff <(echo "$declared") <(echo "$actual"); then
            echo "::error::the top-level surface changed. Lines starting '>' are"
            echo "::error::undeclared additions: adding one is a publication decision."
            echo "::error::If intended, add it to .github/publication-surface.txt."
            exit 1
          fi
          echo "surface matches: $(echo "$actual" | wc -l | tr -d ' ') declared entries"

  audit:
    name: cargo-deny (licenses, advisories, bans, sources)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # deny.toml's [licenses.private] ignore is deliberately `false`: if the
      # crate ever stops declaring its own license, this job FAILS rather than
      # passing quietly.
      - uses: EmbarkStudios/cargo-deny-action@v2
